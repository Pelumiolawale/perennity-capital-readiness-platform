node_modules
dist
.env
.env.local
*.local
.DS_Store
src/regulations/proprietary/

# Claude Code per-developer agent state (session transcripts,
# preview launch config, tool permissions). Never commit.
.claude/
.vercel

# Stray pnpm lockfile — this project uses npm (package-lock.json + `npm ci`).
# A committed pnpm-lock.yaml makes Vercel switch package managers and fail.
pnpm-lock.yaml
