# Copy this file to .env.local and add your key
# .env.local is gitignored — never commit your actual key

# Anthropic API key — enables AI Narrative Panel and Regulatory Q&A
# Get yours at: https://console.anthropic.com/
VITE_ANTHROPIC_API_KEY=sk-ant-...

# IMPORTANT: For production deployment, proxy API calls through a serverless
# function (e.g. Vercel /api/analyse) so this key is never exposed to browsers.
